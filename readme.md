CREATE DATABASE sc_db
    WITH
    OWNER = usr
    ENCODING = 'UTF8'
    LOCALE_PROVIDER = 'libc'
    CONNECTION LIMIT = -1
    IS_TEMPLATE = False;

// display days without smoking
// saved with app

// mark and reset timer